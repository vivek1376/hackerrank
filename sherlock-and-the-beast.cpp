#include <iostream>

int main()
{
    int T, N, m, n = 0;
    cin >> T;

    while (T--)
    {
        while (N >= 5 * n)
        {
            if ( (N - 5 * n) % 3 == 0)
            {
                //output
            }
            n++;

        }
        cout<<"-1"<<endl;
    }
}
